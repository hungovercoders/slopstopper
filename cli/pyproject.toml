[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "slopstopper-cli"
version = "0.1.0"
description = "Slopstopper quality suite — checks, report generation, GH-API emission."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Richard Griffiths" }]
keywords = ["ci", "quality", "checks", "linting", "slopstopper"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Quality Assurance",
]
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7"]

[project.scripts]
slopstopper = "slopstopper.cli:main"

[project.urls]
Homepage = "https://slopstopper.dev"
Source = "https://github.com/hungovercoders/slopstopper"

[tool.hatch.build.targets.wheel]
packages = ["slopstopper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
