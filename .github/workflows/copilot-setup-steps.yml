name: "Copilot Setup Steps"

# This workflow configures the environment for GitHub Copilot Agent with gh-aw MCP server
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called 'copilot-setup-steps' to be recognized by GitHub Copilot Agent
  copilot-setup-steps:
    runs-on: ubuntu-latest

    # Set minimal permissions for setup steps
    # Copilot Agent receives its own token with appropriate permissions
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up slopstopper toolchain
        # Installs node (and task) from mise.toml ([tools]) onto PATH, so the
        # Node version is single-sourced from the pin rather than hardcoded here.
        uses: jdx/mise-action@v2

      - name: Install npm dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Install Taskfile
        run: |
          sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b /usr/local/bin

      - name: Install Python tools (for complexity/security analysis)
        run: pip install lizard bandit semgrep

      - name: Install gh-aw extension
        uses: github/gh-aw/actions/setup-cli@v0.50.7
        with:
          version: v0.50.7
