name: SlopStopper · Deploy to Netlify

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to Netlify (Production)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: nwtgck/actions-netlify@v2.1
        with:
          publish-dir: './app'
          production-deploy: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions - ${{ github.event.head_commit.message }}"
          enable-pull-request-comment: false
          enable-commit-comment: true
          overwrites-pull-request-comment: false
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        timeout-minutes: 5

      # PR Preview Deployment with Before/After Screenshots
      - name: Install Playwright
        if: github.event_name == 'pull_request'
        run: |
          npx playwright install chromium

      - name: Start local server for "Before" screenshot
        if: github.event_name == 'pull_request'
        run: |
          # Checkout main branch to a separate directory and build it
          git fetch origin main
          rm -rf ../main-branch
          git clone --single-branch --branch main $GITHUB_WORKSPACE ../main-branch
          cd ../main-branch
          npm ci
          npm run build
          PORT=8080 node server.js &
          echo $! > /tmp/server_before.pid
          sleep 5
          # Wait for server to be ready
          for i in {1..10}; do
            if curl -s http://localhost:8080 > /dev/null; then
              echo "Server is ready"
              exit 0
            fi
            sleep 1
          done
          echo "Server failed to start"
          exit 1

      - name: Take "Before" screenshot (main branch)
        if: github.event_name == 'pull_request'
        run: |
          npx playwright screenshot --browser chromium http://localhost:8080 before-screenshot.png
          mv before-screenshot.png $GITHUB_WORKSPACE/

      - name: Stop "Before" server
        if: github.event_name == 'pull_request'
        run: |
          if [ -f /tmp/server_before.pid ]; then
            kill $(cat /tmp/server_before.pid) || true
            rm /tmp/server_before.pid
          fi

      - name: Start local server for "After" screenshot
        if: github.event_name == 'pull_request'
        run: |
          cd $GITHUB_WORKSPACE
          PORT=8081 node server.js &
          echo $! > /tmp/server_after.pid
          sleep 5
          # Wait for server to be ready
          for i in {1..10}; do
            if curl -s http://localhost:8081 > /dev/null; then
              echo "Server is ready"
              exit 0
            fi
            sleep 1
          done
          echo "Server failed to start"
          exit 1

      - name: Take "After" screenshot (PR branch)
        if: github.event_name == 'pull_request'
        run: |
          npx playwright screenshot --browser chromium http://localhost:8081 after-screenshot.png

      - name: Stop "After" server
        if: github.event_name == 'pull_request'
        run: |
          if [ -f /tmp/server_after.pid ]; then
            kill $(cat /tmp/server_after.pid) || true
            rm /tmp/server_after.pid
          fi

      - name: Upload screenshots as artifacts
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: website-screenshots
          path: |
            before-screenshot.png
            after-screenshot.png

      - name: Deploy to Netlify (Preview)
        if: github.event_name == 'pull_request'
        id: netlify-preview
        uses: nwtgck/actions-netlify@v2.1
        with:
          publish-dir: './app'
          production-deploy: false
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy preview from PR #${{ github.event.number }}"
          enable-pull-request-comment: false
          enable-commit-comment: false
          overwrites-pull-request-comment: false
          alias: pr-${{ github.event.number }}
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        timeout-minutes: 5

      - name: Comment PR with preview URL and screenshots
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            // Check if screenshot files exist
            if (!fs.existsSync('before-screenshot.png') || !fs.existsSync('after-screenshot.png')) {
              console.error('Screenshot files not found. before-screenshot.png:', fs.existsSync('before-screenshot.png'), 'after-screenshot.png:', fs.existsSync('after-screenshot.png'));
              core.setFailed('Screenshot files were not created successfully');
              return;
            }

            const beforeImage = fs.readFileSync('before-screenshot.png').toString('base64');
            const afterImage = fs.readFileSync('after-screenshot.png').toString('base64');

            const comment = `## 🚀 Netlify Preview Deployment

            ### 🔗 Preview URL
            **${{ steps.netlify-preview.outputs.deploy-url }}**

            ---

            ### 📸 Visual Comparison

            #### Before (main branch)
            ![Before Screenshot](data:image/png;base64,${beforeImage})

            #### After (this PR)
            ![After Screenshot](data:image/png;base64,${afterImage})

            ---

            *Deployed from commit ${{ github.event.pull_request.head.sha }}*`;

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existingComment = comments.find(comment =>
              comment.user.login === 'github-actions[bot]' &&
              comment.body.includes('🚀 Netlify Preview Deployment')
            );

            if (existingComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: comment
              });
            } else {
              // Create new comment
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment
              });
            }
